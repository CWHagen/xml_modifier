import math
class Element:
	def __init__(self, is_curved, length, height, radius, theta):
		self.is_curved = is_curved
		self.length = length
		self.height = height			
		self.radius = radius
		self.theta = theta

	# def calculate_curve(self):
	# 	self.is_curved = self.radius is not None
	# 	if self.is_curved:
	# 		self.theta = float(self.length)/float(self.radius)
		
	# def calculate_curve(self)
	# 	self.theta = self.length / self.radius



	
